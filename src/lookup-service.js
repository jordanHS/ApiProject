export class PokemonSearch {
    async findPokemon(type) {
        let response = await fetch(`ç`);
        let jsonifiedResponse = await response.json();
        return jsonifiedResponse;
        }
    }